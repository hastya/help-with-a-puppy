{
  "name": "help-with-a-puppy-mobile",
  "version": "1.0.0",
  "description": "Standalone offline Android build of Help with a puppy (Capacitor wrapper).",
  "private": true,
  "scripts": {
    "add:android": "cap add android",
    "sync": "cap sync android",
    "build:apk": "cap sync android && cd android && ./gradlew assembleDebug",
    "open:android": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
